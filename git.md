```
git config --global --list

alias.co=checkout
alias.st=status
alias.fa=fetch --all
alias.mm=!git fetch origin master && git merge origin/master
```

gitconfig
```
alias.co=checkout
alias.st=status
alias.fa=fetch --all
alias.mm=!git fetch origin master && git merge origin/master
```
